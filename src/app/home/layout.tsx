// "use client";

import { Metadata } from "next";
import Header from "@/src/components/Header";
import DashBoardLayout from "@/src/components/DashBoardLayout";
import AnimateHeader from "@/src/components/AnimateHeader/AnimateHeader";
import Link from "next/link";
import HomeTransitionProvider from "@/src/components/HomeTransitionProvider";
import { AnimatePresence, motion } from "framer-motion";
import { usePageMounted } from "@/src/hooks/useTimeLine";

// export const metadata: Metadata = {
//   title: "Home Page",
//   description: "Generated by Next.js",
// };

export default function HomeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // console.log("children:", children);
  // const isOpen = usePageMounted(state => state.isMounted);
  // const setIsMounted = usePageMounted(st => st.setIsMounted);

  return (
    <>
      {/* <button  onClick={() => setIsMounted(true)}>DTRUE</button>{" "}
    <button  onClick={() => setIsMounted(false)}>DFALSE</button> */}

      {/* {isOpen && <HomeTransitionProvider> */}
      {/* <AnimateHeader> */}
      <Header />
      {/* </AnimateHeader> */}
      <Link style={{ color: "red", fontSize: 20 }} href={"/"}>
        Main
      </Link>

      <DashBoardLayout>{children}</DashBoardLayout>
    </>
  );
}
